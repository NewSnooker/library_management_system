/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['github.com', 'avatars.githubusercontent.com',"images.unsplash.com","plus.unsplash.com","utfs.io"], // Add other domains as needed
      },
};

export default nextConfig;
